version: "3.9"

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: orbit
      POSTGRES_USER: orbit
      POSTGRES_PASSWORD: orbit
    ports:
      - "${ORBIT_POSTGRES_PORT:-5432}:5432"
    volumes:
      - orbit-postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U orbit -d orbit"]
      interval: 10s
      timeout: 5s
      retries: 5

  orbit-api:
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      postgres:
        condition: service_healthy
      otel-collector:
        condition: service_started
    environment:
      ORBIT_API_HOST: 0.0.0.0
      ORBIT_API_PORT: 8000
      ORBIT_AUTO_MIGRATE: "true"
      MDE_DATABASE_URL: postgresql+psycopg://orbit:orbit@postgres:5432/orbit
      ORBIT_JWT_SECRET: orbit-dev-secret-change-me
      ORBIT_JWT_ISSUER: orbit
      ORBIT_JWT_AUDIENCE: orbit-api
      ORBIT_RATE_LIMIT_EVENTS_PER_DAY: 100
      ORBIT_RATE_LIMIT_QUERIES_PER_DAY: 500
      ORBIT_RATE_LIMIT_PER_MINUTE: 1000/minute
      ORBIT_OTEL_SERVICE_NAME: orbit-api
      ORBIT_OTEL_EXPORTER_ENDPOINT: http://otel-collector:4318/v1/traces
    ports:
      - "8000:8000"

  prometheus:
    image: prom/prometheus:latest
    command: ["--config.file=/etc/prometheus/prometheus.yml"]
    volumes:
      - ./deploy/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./deploy/prometheus/alerts-orbit.yml:/etc/prometheus/alerts-orbit.yml:ro
    ports:
      - "9090:9090"
    depends_on:
      - orbit-api
      - alertmanager

  alertmanager:
    image: prom/alertmanager:latest
    command:
      - "--config.file=/etc/alertmanager/alertmanager.yml"
      - "--config.expand-env"
    environment:
      ALERTMANAGER_DEFAULT_WEBHOOK_URL: "${ALERTMANAGER_DEFAULT_WEBHOOK_URL:-http://127.0.0.1:65535/noop}"
      ALERTMANAGER_SLACK_WEBHOOK_URL: "${ALERTMANAGER_SLACK_WEBHOOK_URL:-http://127.0.0.1:65535/slack}"
      ALERTMANAGER_SLACK_CHANNEL: "${ALERTMANAGER_SLACK_CHANNEL:-#orbit-alerts}"
      ALERTMANAGER_EMAIL_TO: "${ALERTMANAGER_EMAIL_TO:-alerts@example.com}"
      ALERTMANAGER_EMAIL_FROM: "${ALERTMANAGER_EMAIL_FROM:-orbit-alerts@example.com}"
      ALERTMANAGER_EMAIL_SMARTHOST: "${ALERTMANAGER_EMAIL_SMARTHOST:-localhost:1025}"
      ALERTMANAGER_EMAIL_USERNAME: "${ALERTMANAGER_EMAIL_USERNAME:-}"
      ALERTMANAGER_EMAIL_PASSWORD: "${ALERTMANAGER_EMAIL_PASSWORD:-}"
    volumes:
      - ./deploy/alertmanager/alertmanager.yml:/etc/alertmanager/alertmanager.yml:ro
    ports:
      - "9093:9093"

  otel-collector:
    image: otel/opentelemetry-collector-contrib:latest
    command: ["--config=/etc/otelcol-contrib/config.yaml"]
    volumes:
      - ./deploy/otel/otel-collector-config.yaml:/etc/otelcol-contrib/config.yaml:ro
    ports:
      - "4317:4317"
      - "4318:4318"

volumes:
  orbit-postgres-data:
