global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/alerts-orbit.yml

scrape_configs:
  - job_name: "orbit-api"
    metrics_path: /v1/metrics
    static_configs:
      - targets: ["orbit-api:8000"]
